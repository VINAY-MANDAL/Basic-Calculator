import random

length = int(input("Password length: "))

characters = "abcdefghijklmnopqrstuvwxyz0123456789.!@#$%&*_+-*/."

password = ""

for i in range(length):
    password += random.choice(characters)

print("Generated password:", password)vvv
